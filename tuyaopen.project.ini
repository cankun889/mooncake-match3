; TuyaOpen project descriptor
; Maintained by the TuyaOpen IDE extension.
; Hand-editing is allowed — unknown keys are preserved on round-trips.

[project]
name = moon
version = 0.1.0
created = 2026-09-23T13:08:08.298Z
type = tuyaopen-app
; INI format version — do not edit
schema = 1

[platform]
; valid targets: t5 | esp32 | linux | raspberrypi
target = t5ai
sdk = tuyaopen
sdk_version = main

[build]
output = build/
toolchain = default

[board]
id = tuya-t5ai-board
kconfig_id = TUYA_T5AI_BOARD

[features]
description = 中秋月饼消消乐，跑在 T5AI 3.5 寸触摸屏上。
